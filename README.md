`pkg update && pkg upgrade`

`pkg install nodejs`

`mkdir rakibul-bot`

`cd rakibul-bot`

`npm install 
@whiskeysockets/baileys@latest`

`pkg install openssl -y`

'node -v'

'npm -v`

`npm init -y`

`nano index.js`
